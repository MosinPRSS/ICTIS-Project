// Набор штучек для конвертации всяких 
// форматированных описаний в HTML-понятный формат
// из Markdown

// Логика: *text* -> <b>text</b>
// `code` -> <code>code</code
// и т.д., однако потребуется создать в две стороны:
// из HTML в MD и из MD в HTML

export function toMarkDown(text: string) {}

export function toHTML(text: string) {}