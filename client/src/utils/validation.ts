import { IValidateData } from "@/interfaces/interfaces";

export default function validation({
	name,
	email,
	password,
	submitPassword,
}: IValidateData) {
	let isValidate = true;
	return isValidate;
}
