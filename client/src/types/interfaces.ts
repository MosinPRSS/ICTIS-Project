export interface HeaderProps {
  children?: React.ReactNode;
  className?: string;
}
export interface bot {
    name: string
    description: string
    author: string
    image: string
    chatsCount: number
    tags: string[]
}